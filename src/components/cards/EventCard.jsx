import MediaCard from './MediaCard';

const EventCard = (props) => <MediaCard gradientClass="from-[var(--primary-500)] to-[var(--primary-400)]" subtitleWordsLimit={10} {...props} />;

export default EventCard;
